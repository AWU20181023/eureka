spring:
  application:
    name: eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/, http://localhost:8762/eureka/,
    register-with-eureka: false  #关闭将报连接异常
  #    暴露ip地址
  instance:
    prefer-ip-address: true
#  server:
#    enable-self-preservation: false  关闭自我保护
#server:
#  port: 8761
